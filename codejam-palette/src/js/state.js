const state = {
  currentTool: 'pencil',
  isShowRules: true,
  baseSize: 512,
  currentSize: 4,
  currentSource: './assets/data/4x4.json',
  maxGridCapacity: 64,
};

export default state;

// paint-bucket
// choose-color
// pencil
